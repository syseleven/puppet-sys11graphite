#! /bin/bash --
# This file generated by puppet, do not edit here.

# Parameter values from puppet template
DIR="<%= $whisper_datadir -%>"
DAYS="<%= $whisper_expire_days_delay -%>"

cd "$DIR" && find . -type f -mtime "$DAYS" -exec rm {} \;

# regenerates $DIR/../index, file must be owned by graphite-user (e.g. www-data)
/opt/graphite/bin/build-index.sh
